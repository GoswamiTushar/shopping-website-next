import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: 'red'
        }
    },
    typography: {
        fontFamily: 'Nunito Sand, sans-serif'
    }
})